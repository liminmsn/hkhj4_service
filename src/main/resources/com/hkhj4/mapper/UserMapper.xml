<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hkhj4.mapper.UserMapper">

    <insert id="createUser" parameterType="com.hkhj4.entity.TbUser">
        insert into tb_user (username, password, email, image, gender, register_time, create_time, update_time)
        VALUES (#{username}, #{password}, #{email}, #{image}, #{gender}, now(), now(), now());
    </insert>
    <update id="updateUserPassword">
        UPDATE tb_user
        SET password = #{password},
        update_time = now()
        WHERE email = #{email}; <!-- 按你之前的需求：邮箱存在时更新 -->
    </update>
</mapper>